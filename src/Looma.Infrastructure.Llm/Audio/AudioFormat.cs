namespace Looma.Infrastructure.Llm.Audio;

/// <summary>The two audio container formats Looma can decode (see <see cref="AudioMediaTypeSniffer"/>).</summary>
public enum AudioFormat
{
    Wav,
    Mp3
}
